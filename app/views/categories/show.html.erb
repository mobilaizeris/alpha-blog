<h1 align="center"><%= "Category: " + @category.name %></h1>
<% if current_user.admin? and logged_in? %>
	<div align="center">
		<span type="button badge" class="btn"><%= link_to "Edit Category name", edit_category_path(@category)%></span>
	</div>
<% end -%>
<div align="center">
	<%= will_paginate @category_articles %>
</div>
<%= render 'articles/article', obj: @category_articles	%>
<div align="center">
	<%= will_paginate @category_articles %>
</div>